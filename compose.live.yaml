services:
  orchestrator:
    environment:
      APP_MODE: live
      DATABASE_PATH: /data/live-orchestrator.db
      DEVIN_API_KEY: "${DEVIN_API_KEY:?DEVIN_API_KEY is required}"
      DEVIN_ORG_ID: "${DEVIN_ORG_ID:?DEVIN_ORG_ID is required}"
      DEVIN_MAX_ACU_LIMIT: "${DEVIN_MAX_ACU_LIMIT:-3}"
      DEVIN_BYPASS_APPROVAL: "${DEVIN_BYPASS_APPROVAL:-false}"
      GITHUB_WEBHOOK_SECRET: "${GITHUB_WEBHOOK_SECRET:?GITHUB_WEBHOOK_SECRET is required}"
      CONTROL_PLANE_USERNAME: "${CONTROL_PLANE_USERNAME:-operator}"
      CONTROL_PLANE_PASSWORD: "${CONTROL_PLANE_PASSWORD:?CONTROL_PLANE_PASSWORD is required}"
      POLL_INTERVAL_SECONDS: "${POLL_INTERVAL_SECONDS:-15}"
      WORKER_STALE_AFTER_SECONDS: "${WORKER_STALE_AFTER_SECONDS:-60}"
      MAX_ACTIVE_SESSIONS: "${MAX_ACTIVE_SESSIONS:-3}"
      CREATE_RETRY_INTERVAL_SECONDS: "${CREATE_RETRY_INTERVAL_SECONDS:-60}"
